{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "outputs": [],
      "dependsOn": ["build", "^build"],
      "inputs": ["$TURBO_DEFAULT$", "../../test/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "coverage": {
      "dependsOn": ["build", "^build"],
      "outputs": ["coverage/**"]
    }
  }
}
