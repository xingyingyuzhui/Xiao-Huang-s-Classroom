# 工程基线冻结（2026-08-07）

> Program 0 Task 0.1 产物。数据为执行当日实测，作为迁移前后对比的权威基线。
> 更新基线时保留历史行，不覆盖旧值。

## 1. 测试矩阵

| 范围                                            | 结果                         |
| ----------------------------------------------- | ---------------------------- |
| 全仓 `npm test`（node:test，83 个测试文件）     | 494/494 PASS，0 fail，0 skip |
| 数学 `test/web/math-*.test.cjs`                 | 313/313 PASS，0 fail，0 skip |
| 新增生命周期测试（mount-controller / readouts） | 24/24 PASS                   |

## 2. 构建与产物

| 项                          | 值                                                                                     |
| --------------------------- | -------------------------------------------------------------------------------------- |
| `npm run build`（Vite web） | PASS；时长 ~2s                                                                         |
| 既有 warning                | JSXGraph `eval`（jessiecode.js）、`mathviz` 大 chunk（>500kB）——允许清单，禁止新增类型 |
| 产物根                      | `apps/web/dist/`（生成目录，禁止作为源改动）                                           |

主要 chunk（min 后 / gzip 后）：

| chunk                     | size (kB)       | gzip (kB)    |
| ------------------------- | --------------- | ------------ |
| mathviz                   | 1,286           | 337          |
| three                     | 690             | 176          |
| index（hub 等）           | 304 / 186 / 131 | 89 / 58 / 40 |
| animation / OrbitControls | 51 / 19         | 20 / 4       |

### 2a. 2026-08-08 Task 8：拆包 + 路径预算基线（mathviz → jsxgraph/katex 拆分）

拆分前（`mathviz: ['jsxgraph', 'katex']` 单 chunk，实测值记执行日志）：

| 路径                                  | raw (kB) | gzip (kB) | 请求数 |
| ------------------------------------- | -------: | --------: | -----: |
| initial（index.html 闭包）            |    1,247 |       307 |      3 |
| mathGraph 增量                        |    1,452 |       389 |      5 |
| mathClassroomKatexOnly 增量（含字体） |    2,350 |     1,187 |     63 |

拆分后（`three/jsxgraph/katex/animation` 独立 manualChunks + Vite `manifest: true`）：

| chunk（family）  | raw (kB) | gzip (kB) |
| ---------------- | -------: | --------: |
| jsxgraph         |      967 |       250 |
| katex            |      254 |        75 |
| three            |      673 |       171 |
| index（JS 合计） |      615 |       192 |
| total（JS+CSS）  |    3,049 |       820 |

| 路径                                  | raw (kB) | gzip (kB) | 请求数 |
| ------------------------------------- | -------: | --------: | -----: |
| initial（index.html 闭包）            |    1,247 |       307 |      3 |
| mathGraph 增量                        |    1,163 |       312 |      5 |
| mathClassroomKatexOnly 增量（含字体） |    1,349 |       935 |     63 |

效果：mathGraph 增量 -289kB raw；KaTeX-only 路径 -1,002kB raw（不再拉 JSXGraph）。
预算阈值见 `tooling/performance/budget.json`（实测 +5%~8% 字节余量、请求数 +≤1），
请求集合拆分前 fixture 见 `tooling/performance/route-request-baseline.json`（版本化，非阈值）。

Warning 状态（Task 8 验收）：3 条「动态 + 静态同时导入」warning 归零（classroom-loader、
object-style-panel、board-compass 的假 lazy 动态导入已改静态）；JSXGraph JessieCode
`eval` warning 仅剩 node_modules（jessiecode.js）；jsxgraph 990kB 大 chunk warning 保留，
由 `budget.json` jsxgraph 预算解释（禁止提高 `chunkSizeWarningLimit` 掩盖）。
结构合同由 `test/web/dynamic-import-boundary.test.cjs` 锁定。

## 3. 源码规模

| 范围                                                                    | 文件数                        | 行数                 |
| ----------------------------------------------------------------------- | ----------------------------- | -------------------- |
| 全仓 JS 源（apps+packages+scripts+test，排除 dist/public/node_modules） | 250                           | ~60,932              |
| apps/web/src                                                            | 188                           | —                    |
| apps/server/src                                                         | 51                            | —                    |
| apps/desktop                                                            | 0（main.cjs 单文件，无 src/） | —                    |
| packages/math-expr、subject-settings                                    | 0（纯 JS 直接文件，无 src/）  | —                    |
| graph/index.js（函数画布入口）                                          | —                             | 690（结构门禁 <700） |

## 4. 依赖图摘要

根 workspaces：`apps/web`、`apps/server`、`apps/desktop`、`packages/math-expr`、`packages/subject-settings`。

- web → math-expr、subject-settings（workspace 内联）
- server → math-expr、subject-settings、express、sql.js、cors、pkg
- web 运行时主要三方依赖：three@0.172、jsxgraph@1.13、katex、animejs、vite（构建）
- 工具：electron@33（含 Node 20 基线）、electron-builder

## 5. 环境

| 项           | 值                                                                   |
| ------------ | -------------------------------------------------------------------- |
| 本机 Node    | v24.18.0                                                             |
| 目标最低基线 | Electron 33 内嵌 Node 20；pkg 过渡窗口保持 ES2022/Node 18 可执行子集 |
| 包管理器     | npm workspaces（根 package-lock.json 单一）                          |
| 质量工具现状 | 无 ESLint/Prettier/Stylelint/Turborepo/CI；测试仅 node:test          |
| git          | 72 个提交（含本分支全部历史）；无 tag 基线                           |

## 6. 用户数据位置（三类）

| 位置                         | 现状                                               | 迁移期处理                                    |
| ---------------------------- | -------------------------------------------------- | --------------------------------------------- |
| Web 开发 `apps/server/data/` | chem-lab.db 等；`apps/server/src/data/` 为历史路径 | 识别但不作为新写入目标                        |
| Electron `userData/data`     | 由 main.cjs 定位                                   | 迁移框架必须支持                              |
| pkg 便携版邻近 `data`        | 由 isPkg 分支定位                                  | 过渡窗口保持；退役后由 Electron portable 替代 |

## 7. 行为兼容清单摘要（详见 behavior-compatibility.md）

大厅全出血、书籍 intro/cover-dissolve、`/api/...` 路径、五主题、`chem-theme-change`、化学实验行为、函数画布合同、Electron 用户数据路径。
